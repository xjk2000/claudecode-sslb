---
description: "创建新敕令 - 启动三省六部多Agent协作流程"
---

委托给 @中书令 处理。

中书令启动后：

1. **Phase 0** — 检查 `docs/huangdi/政事堂/` 是否存在，不存在则创建完整目录结构
2. **判断用户是否附带了任务描述**：
   - **有任务描述** → 直接进入 Phase 1（需求探索）
   - **无任务描述**（用户仅输入 `/sslb:new-edict`）→ 完成初始化后，询问：**「陛下，您的旨意是什么？」**，等待用户输入
3. **Phase 1** — 需求探索（brainstorming），逐步厘清需求
4. **Phase 2** — 获得用户确认后，拆解为标准化敕令
5. **Phase 3** — 将敕令提交给 @侍中（门下省）审议
6. **Phase 4** — 审议通过后交给 @尚书令 制定实现计划并调度六部五监执行

**不要跳过任何步骤。整个流程由中书令主导。**
